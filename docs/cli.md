# CLI for demo-go-service

## Help

Get the cli help by running `bin/demo-go-service help` 

```bash
A longer description that spans multiple lines and likely contains
examples and usage of using your application. For example:

Cobra is a CLI library for Go that empowers applications.
This application is a tool to generate the needed files
to quickly create a Cobra application.

Usage:
  generated [command]

Available Commands:
  help        Help about any command
  server      A brief description of your command
  version     Print the version number of generated code example

Flags:
  -h, --help   help for generated

Use "generated [command] --help" for more information about a command.
```
